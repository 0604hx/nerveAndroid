<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.nerve"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="7" android:targetSdkVersion="15" />

    <application android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme">
		<activity
            android:name=".example.corner.ConrnerActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar"
            android:label="@string/app_name" >
            
        </activity>
        <activity
            android:name=".example.corner.SelectProvinceActitivyctitivy"
            android:theme="@android:style/Theme.Light.NoTitleBar"
            android:label="@string/app_name" >
        </activity>
        <activity
             android:name=".example.corner.SelectTimeActitivyctitivy"
            android:theme="@android:style/Theme.Light.NoTitleBar"
            android:label="@string/app_name" >
        </activity>
        
        <activity android:name=".example.dialog.DialogActivity">
        </activity>
        
        <activity android:name=".example.list.MyListActivity"
            android:windowSoftInputMode="adjustPan">
             <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
